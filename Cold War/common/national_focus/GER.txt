focus_tree = {
	id = german_focus_tree
	
	country = {
		factor = 0
		
		modifier = {
			add = 10
			tag = GER
			#always = no
		}
	}
	
	default = no
	continuous_focus_position = { x = 43 y = 155 }
	
	focus = {
		id = GER_start_nuremberg_trials
		available = {
		}
		bypass = {
		}
		
		icon = GFX_focus_generic_self_management
		x = 10
		y = 1
		cost = 40

		available_if_capitulated = no

		completion_reward = {
			hidden_effect = { country_event = cw_historic_events.20 }
			add_timed_idea = {
				idea = nuremberg_trials
				days = 120
			}
		}
		ai_will_do = {
			base = 1
			modifier = {
				add = 100
				is_historical_focus_on = yes
			}
		}		
	}
}